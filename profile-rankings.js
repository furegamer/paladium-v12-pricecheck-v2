(() => { const boot=()=>{const p=window.PriceCheckPlayers;if(!p)return;const tabs=document.querySelector('.tabs'),board=document.querySelector('#board');if(!tabs||!board)return;let pbTab=document.querySelector('[data-rank="pb"]');if(!pbTab){pbTab=document.createElement('button');pbTab.className='tab';pbTab.dataset.rank='pb';pbTab.textContent='🪙 PB';tabs.appendChild(pbTab)}const labels={wallet:'wallet',pb:'pb',miner:'miner_level',farmer:'farmer_level',hunter:'hunter_level',alchemist:'alchemist_level',pog:'pog_level'};const labelName=k=>({wallet:'Portefeuille',pb:'Paladium Blocks',miner:'Métier Mineur',farmer:'Métier Fermier',hunter:'Métier Chasseur',alchemist:'Métier Alchimiste',pog:'Niveau POG'})[k]||k;const render=async key=>{try{const rows=await p.api(`player_stats?select=player_id,display_name,wallet,pb,miner_level,farmer_level,hunter_level,alchemist_level,pog_level&order=${labels[key]}.desc&limit=50`);const top=rows.slice(0,3);while(top.length<3)top.push(null);board.innerHTML=top.map((r,i)=>r?`<div class="row ${r.player_id===p.getId()?'me':''}"><span class="rank">#${i+1}</span><span class="name">${String(r.display_name||'Joueur').replace(/[<>]/g,'')} ${r.player_id===p.getId()?'(toi)':''}<small>${labelName(key)}</small></span><span class="value">${key==='wallet'?`${Number(r.wallet||0).toLocaleString('fr-FR')} P$`:key==='pb'?`${Number(r.pb||0).toLocaleString('fr-FR')} PB`:`Niv. ${r[labels[key]]||1}`}</span></div>`:`<div class="row"><span class="rank">#${i+1}</span><span class="name">Place disponible<small>En attente d'un joueur</small></span><span class="value">—</span></div>`).join('')}catch{board.innerHTML='<div class="notice">⚠️ Classement momentanément indisponible.</div>'}};pbTab.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));pbTab.classList.add('active');render('pb')};document.querySelectorAll('.tab').forEach(b=>{const old=b.onclick;b.onclick=()=>{document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');render(b.dataset.rank)}});const active=document.querySelector('.tab.active');if(active)render(active.dataset.rank||'wallet')};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();
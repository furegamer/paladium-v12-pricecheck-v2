(() => { const boot=async()=>{const p=window.PriceCheckPlayers;if(!p)return;await p.hydrateFromServer();const input=document.querySelector('#budget'),balance=document.querySelector('#balance');if(!input)return;const sync=()=>{const wallet=p.getWallet();input.value=String(wallet);if(balance)balance.textContent=`${wallet.toLocaleString('fr-FR')} P$`;input.title='Budget synchronisé avec le portefeuille de ton profil';const label=input.previousElementSibling;if(label)label.textContent='Budget / portefeuille synchronisé';};sync();input.addEventListener('input',()=>{p.setWallet(input.value);sync();});setInterval(async()=>{await p.hydrateFromServer();sync()},15000)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot()})();